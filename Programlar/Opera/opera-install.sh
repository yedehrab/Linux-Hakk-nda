# Güncellemeleri kontrol etme
sudo apt-get update

# Gereksinimleri kurma
sudo apt-get install libcurl3

# Operayı kurma
sudo dpkg -i opera.deb

# Flash dizinine girme
cd flash/

# Flash player'i gereken yere taşıma
sudo mkdir /usr/lib/adobe-flashplugin/
sudo cp -r * /usr/lib/adobe-flashplugin/